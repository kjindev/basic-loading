name: TestCode Test

on:
  # workflow_dispatch:
  # push:
  #   branches:
  #     - release
  push:
    branches:
      - develop

jobs:
  Run-Jest:
    runs-on: [ubuntu-latest]
    # strategy:
    #   matrix:
    #     node-version: [20.x]
    steps:
      - uses: actions/checkout@v2
      - name: Setting Dependencies
        run: npm install
      - name: Run Test Code
        run: npm run test
